# 実装計画 - Michael Jackson Footsteps Beat Trainer（iPhone版）

## 前提（ヒアリング結果）
- 開発機材：Mac無し。Windows PCのみ。実機テストは自分のiPhoneで行う。
- 予算：クレカ登録不要・無料の範囲で完結（Apple Developer Program(年間$99)には加入しない）。
- 技術方針：企画書に忠実なネイティブSwift/SwiftUIを採用（案A）。
- ビルド：GitHub Actions（macos-latestランナー、無料枠）でxcodebuild。
- 実機配布：AltStore/SideStore（無料Apple ID・クレカ不要）でサイドロード。AltServerはWindows対応なので、初回ペアリングも今のPCで完結する（Mac借用は不要）。
- MVP範囲：最小構成。①曲読み込み ②簡易BPM/拍検出 ③拍に合わせて振動 ④画面に足跡アイコン表示（なぞり判定なし）。指の追従判定・結果表示・モード切替・ARモードは後日追加。

## 方針転換（2026-08-04）
- 曲の読み込み方式を変更：端末のミュージックライブラリから都度選ぶ方式（`MPMediaPickerController`）を廃止し、**楽曲データをアプリのビルドに内蔵（バンドル）する方式**に切り替えた。
- 理由：ユーザーがPCでダウンロードした楽曲ファイルを直接アプリに組み込みたいという要望のため。個人の端末で個人が使う範囲を前提とする（企画書のUXステップ2「個人的に所持している曲を読み込む」に合致）。
- 受け渡しフロー：ユーザーが `inbox/music/` に楽曲ファイル（mp3/m4a/wav/aac）を配置 → Claudeが `Resources/Music/` に取り込み → `project.yml` のフォルダ参照経由でXcodeビルドに自動的に含まれる。
- 現状（このコミット時点）：`Resources/Music/` はまだ空。ユーザーからの楽曲提供待ち。
- 運用方針：実機（AltStoreインストール・ハプティクス/同期の体感確認）でのチェックは毎サイクル必須にせず、**コード実装 → GitHub Actionsのビルド成功確認**を基準に次のフェーズへ進めてよいと合意済み。実機での最終確認はユーザーが都合の良いタイミングでまとめて行う。

## 技術スタック
| 項目 | 内容 |
|---|---|
| 言語/UI | Swift 5.9+ / SwiftUI |
| 音楽再生・解析 | AVFoundation（AVAudioEngine） |
| 曲選択 | アプリ内蔵（Resources/Musicのバンドルリソースを起動時に列挙） |
| ハプティクス | CoreHaptics（CHHapticEngine） |
| 最低対応OS | iOS 17（Taptic EngineはiPhone 8以降） |
| CI/ビルド | GitHub Actions（macos-latest） |
| 実機配布 | AltStore / SideStore（無料Apple ID） |

## マイルストーン

### Phase 0：環境・パイプライン構築（最優先）
- Windows PCにAltServerをインストールし、無料Apple IDでペアリング
- iPhoneにAltStore（またはSideStore）をインストール
- GitHub ActionsでXcodeプロジェクトをビルドし、unsigned .ipaを生成するワークフローを作成
- ビルド→AltStore経由で実機インストールまでの一連の流れを、空のSwiftUIアプリで疎通確認

### Phase 1：ハプティクス動作確認
- UIImpactFeedbackGenerator/CHHapticEngineで単発振動を実機で鳴らす
- Phase 0のパイプラインで最新ビルドが実機に届くことを再確認

### Phase 2：曲読み込み（内蔵方式に変更済み）
- `Resources/Music/`（Xcodeフォルダ参照）にバンドルした楽曲ファイルを、アプリ起動時に列挙するUIを実装
- 選択した曲をAVAudioEngineで再生できることを確認

### Phase 3：拍（ビート）検出
- 選択した曲の波形をオフラインで解析し、拍のタイムスタンプ配列を生成
- エネルギーピーク検出などの簡易アルゴリズムで実装（本格的なBPM解析ライブラリは使わず、割り切った精度で進める）

### Phase 4：ハプティクス同期再生
- 拍タイムスタンプに合わせてCHHapticEngineの振動イベントを事前スケジューリング
- 曲の再生時刻とハプティクスのタイミングを同期させる

### Phase 5：足跡アニメーション（なぞり判定なし）
- SwiftUIで足跡アイコンが拍に合わせて画面上を動くアニメーションを実装
- ここまででMVP v1完成

### Phase 6（任意・後日）
- 指のなぞり追従判定（タッチ座標・速度・タイミング解析）
- 結果フィードバック画面（精度％、ズレ傾向、コメント）
- モード切替（ビート重視／ステップ重視／観賞モード）
- ARモード（床に足跡投影）

## リスク・技術的な懸念点
1. **著作権・配布範囲**：内蔵する楽曲はAI生成による、営利目的以外では自由に使える音源（ユーザー確認済み・2026-08-04）。市販曲の著作権リスクはないため、公開リポジトリへのコミットも問題ない。
2. **BPM/拍検出の精度**：専用ライブラリを使わない簡易実装のため、複雑な楽曲では検出精度が落ちる可能性がある。MVPでは精度より「拍に合わせて振動する」体験の成立を優先する。
3. **AltStore/SideStoreの運用コスト**：無料Apple IDでの署名は7日で失効するため、定期的な再署名（SideStoreならWi-Fi/VPN経由で自動化可能、AltStoreなら都度PCとの接続が必要）が発生する。開発中はこの再インストール作業が頻発する前提で運用フローを組む。
4. **GitHub Actionsのビルド時間**：privateだと実質月200分程度に制限されるため、**publicリポジトリで進める方針に決定済み**（ソースコード公開について合意済み）。

## Phase 0 進捗（実装済み）
- `project.yml`：XcodeGen用プロジェクト定義（署名なしビルド設定込み）
- `Sources/HisFootsteps/`：SwiftUI雛形（`HisFootstepsApp.swift` / `ContentView.swift`）＋ `HapticsManager.swift`（CoreHapticsで単発振動を鳴らすPhase1疎通確認用コード）
- `.github/workflows/build.yml`：push時にXcodeGen→xcodebuild→署名なし`.ipa`を生成しArtifactとしてアップロードするワークフロー
- `draft/Phase0_セットアップ手順.md`：AltServer/AltStoreのセットアップ〜実機インストールまでの手順書（ユーザー作業が必要な箇所を明記）

## Phase 2 進捗（内蔵方式へ再実装済み・2026-08-04）
- `project.yml`：`Resources/Music`をフォルダ参照（`type: folder`, `buildPhase: resources`）としてターゲットに追加。`NSAppleMusicUsageDescription`は不要になったため削除
- `Resources/Music/`：アプリに内蔵する楽曲の実体置き場（現在は空、ユーザーからの提供待ち）
- `inbox/music/`：ユーザーが楽曲ファイルを一時的に置く受け渡し場所（README.txt に運用手順を記載）
- `Sources/HisFootsteps/BundledSongLibrary.swift`：Bundle内の`Music`フォルダを走査し、内蔵曲一覧（`BundledSong`）を返す新規ロジック
- `Sources/HisFootsteps/AudioPlayerManager.swift`：`MPMediaItem`ベースの読み込みを廃止し、`BundledSong`（ローカルバンドルURL）を読み込むように変更
- `ContentView.swift`：MPMediaPickerControllerのシート表示を廃止し、内蔵曲一覧から選んで再生するUIに変更
- `Sources/HisFootsteps/MediaPicker.swift`は`archive/Sources_HisFootsteps/`へ退避（削除はせず保管）
- `.github/workflows/build.yml`：曲ファイル追加時もCIが走るよう、pathsトリガーに`Resources/**`を追加

## Phase 3 進捗（実装済み・ビルド未確認・2026-08-04）
- `Sources/HisFootsteps/BeatDetector.swift`：曲の波形をオフラインで解析し、拍タイムスタンプ配列と推定BPMを返すロジックを新規実装
  - アルゴリズム：1024サンプル単位のウィンドウでエネルギー（二乗平均）を計算し、直近約1秒分（43ウィンドウ）の平均エネルギーの1.3倍を超えた瞬間を拍と判定する簡易エネルギーピーク検出方式
  - 連続検出を防ぐため拍と拍の最小間隔を0.2秒（300BPM相当が上限）に設定
- `ContentView.swift`：選択曲に対して「拍を解析」ボタンを追加。バックグラウンドスレッドで解析を実行し、完了後に推定BPMと検出拍数を表示

## Phase 4 進捗（実装済み・ビルド未確認・2026-08-04）
- `HapticsManager.swift`：`playBeatPattern(beatTimestamps:)`を追加。拍タイムスタンプ配列を1つの`CHHapticPattern`（各拍を`relativeTime`付きの`hapticTransient`イベントとして）にまとめて事前スケジューリングし、`start(atTime: CHHapticTimeImmediate)`で再生。`stopBeatPattern()`で途中停止も可能
- `ContentView.swift`：拍検出済みのとき「ビートに合わせて再生」ボタンを追加。押下時に`playBeatPattern`呼び出し直後に`player.play()`を呼ぶことで音楽とハプティクスの再生開始タイミングを近づける簡易同期を実装（フレーム精度の同期は狙わず、割り切った精度で実装。企画書のリスク許容方針に沿う）
- 「停止」ボタンで音楽・ハプティクス両方を停止するよう変更

## Phase 5 進捗（実装済み・ビルド未確認・2026-08-04）
- `Sources/HisFootsteps/BeatScheduler.swift`：拍タイムスタンプ配列に合わせて、再生開始時刻からの相対時間で`DispatchQueue.main.asyncAfter`によりコールバックを呼ぶ簡易スケジューラーを新規実装
- `ContentView.swift`：SF Symbol「shoeprints.fill」を足跡アイコンとして表示し、拍ごとに`.symbolEffect(.bounce)`でバウンス、左右交互にオフセット移動するアニメーションを追加。「ビートに合わせて再生」実行時にスケジューラーを起動し、「停止」時にキャンセル
- **これでPhase 0〜5（MVP v1の最小構成：曲内蔵・再生、拍検出、ハプティクス同期、足跡アニメーション）の実装が一通り完了**。ただしGitHub Actionsでのビルド成功・実機での動作確認はまだ未実施

## 既知の課題：push・CI確認がブロック中（解消済み・2026-08-04）
- このセッションの環境（Claude Codeのシェルプロセス）・ユーザーの別ターミナル・GitHub Desktopのいずれからも`git push`/`git fetch`が`getaddrinfo() thread failed to start`エラーで失敗する状態が続いていた（`ping`でのGitHub到達性は正常だったためDNSではなくWindows側のスレッド生成に関する不具合の可能性が高いと推測）。
- GitHub Desktopでも同一エラーが再現したため、PCの再起動を提案。再起動後にpushが成功し、GitHub Actions上でPhase 2〜5の4コミット分（19eebfe, 9ecee21, 237c786, 0c84177）を含むビルドが実行され、**Run #7（head_sha: 0c84177, conclusion: success）としてビルド成功を確認済み**。
- これによりPhase 2（内蔵方式移行）・Phase 3（拍検出）・Phase 4（ハプティクス同期）・Phase 5（足跡アニメーション）はすべてビルドレベルで正しく積み上がっていることが確認できた。

## Phase 6 進捗（指のなぞり追従判定・実装済み・ビルド成功確認済み・2026-08-04）
- `Sources/HisFootsteps/FootstepPath.swift`：拍インデックスから足跡の目標座標を生成するロジックを新規実装。左右交互（x座標）＋4拍周期の上下ジグザグ（y座標）で簡易的なステップ軌跡を表現
- `Sources/HisFootsteps/FollowTracker.swift`：拍ごとに「指の現在位置」と「足跡の目標座標」の距離を記録し、最大許容距離に対する割合から追従精度（0〜100%）と簡易コメントを算出するロジックを新規実装
  - タイミングのズレ（早い／遅い）は本アルゴリズムでは判定していない（企画書のステップ6の一部機能）。距離ベースの精度判定のみのMVP実装とし、割り切った精度で進める方針に沿った意図的な簡略化
- `ContentView.swift`：足跡の目標位置と指の追従位置を表示する「ステージ」エリア（`followStage`）を追加し、`DragGesture`で指の座標をリアルタイムに取得。「ビートに合わせて再生」実行時に`FollowTracker`をリセットして記録を開始し、最終拍から0.6秒後に自動で追従精度サマリーを表示するようにした
- 企画書の「核となる体験」1点目（指で足跡を追う）に対応する、MVP v1で唯一実装されていなかった中心機能
- **ビルド初回はfailure**（`FollowTracker.swift`に`import Combine`が無く`ObservableObject`/`@Published`が解決できないエラー）。`import Combine`追加で修正し、再pushでビルド成功（head_sha: 80c6fd7, conclusion: success）を確認済み

## Phase 6 進捗（モード切替・実装済み・ビルド未確認・2026-08-04）
- `Sources/HisFootsteps/AppMode.swift`：企画書「機能5：モード切り替え」に対応する`AppMode`（ビート重視／ステップ重視／観賞）を新規実装。各モードにハプティクス強度（`hapticIntensity`）と追従判定の有無（`isFollowJudgeEnabled`）を持たせた
  - ビート重視：振動強め（intensity 1.0）、追従判定あり
  - ステップ重視：標準の振動（intensity 0.7）、追従判定あり
  - 観賞：振動は中間（intensity 0.85）、追従判定なし（操作不要で足跡とビートを眺めるモード）
- `HapticsManager.swift`：`playBeatPattern(beatTimestamps:intensity:)`にintensityパラメータを追加し、モードごとの振動強度を反映できるようにした
- `ContentView.swift`：セグメント式の`Picker`でモードを切り替えられるUIを追加。観賞モードでは`followStage`の操作（`allowsHitTesting`）を無効化し、指の追従記録・精度サマリー表示もスキップするようにした

- モード切替実装分は**ビルド成功を確認済み**（head_sha: eacffeb, conclusion: success）

## Phase 6 進捗（結果フィードバック画面の拡充：タイミングのズレ判定・実装済み・ビルド未確認・2026-08-04）
- `FollowTracker.swift`：指の追従位置を「拍時刻±0.3秒」の履歴（`touchHistory`）から探索し、目標座標に最も近かった瞬間と拍時刻との差分を`timingOffset`（正=遅れ／負=早い）として記録するよう拡張。企画書ステップ6の「タイミングのズレ（早い／遅い）」判定に対応
  - 全拍の平均オフセットが±0.05秒を超える場合に「早め」「遅れ」のコメントを、それ以内なら「よく合っている」コメントを`FollowSummary.timingComment`として算出
  - リアルタイム性を保つため、指の位置履歴は直近1.5秒分のみ保持（メモリ増大を防止）
- `ContentView.swift`：`DragGesture`の`onChanged`で再生開始からの経過時間付きに指の位置履歴を記録するよう変更。「ビートに合わせて再生」開始時に履歴と基準時刻をリセット。結果パネルに`timingComment`を追加表示

## 次のアクション
1. タイミングのズレ判定の実装分について、`git push`→GitHub Actionsのビルド結果を確認する
2. ビルドが失敗していた場合、ログを確認して修正する
3. ビルド確認でき次第、実機で「モード切替→曲再生→拍検出→ビート同期再生→足跡アニメーション→（ビート重視／ステップ重視モードのみ）指なぞり追従→追従精度・タイミングのズレ表示」の一連の体験を確認する
4. 残るPhase 6項目の**ARモード**は、ARKitを用いた実機検証が前提となり、GitHub Actionsのビルド成功だけでは動作を確認しづらい（床への投影・カメラ制御は実機でしか確認できない）ため、実機確認のタイミングでユーザーと相談の上で着手するのが望ましい。他のPhase 6項目（指のなぞり追従判定／結果フィードバック画面／モード切替）は一通り実装完了
5. 以降、実装→（可能であれば）ビルド確認→本ドキュメント更新→次フェーズ実装、のサイクルを継続する
