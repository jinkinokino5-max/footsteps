# CLAUDE.md - プロジェクト作業ルール（スターターキット）

## 0. このファイルの使い方
このフォルダはプロジェクトの雛形（スターターキット）です。新しいプロジェクトを始めるときは、このフォルダごとコピーして目的のカテゴリ（講義用／ワークショップ／開発したアプリたち等）配下にペースト → フォルダ名を変更 → 本ファイルの「1. プロジェクトの目的」をそのプロジェクトに合わせて書き換えてください。2章以降はそのまま使えます（プロジェクト特有のルールが必要なら末尾に章を追加）。

コピー後、最初にやること：
1. フォルダ名をプロジェクト名に変更する
2. 「1. プロジェクトの目的と概要」を書く
3. `git log` で初期コミットが入っていることを確認する（スターターキット自体がgit管理済みなので、コピーした時点でそのまま使えます）

## 1. プロジェクトの目的と概要
（ここにプロジェクト固有の目的・背景を記入）

## 2. フォルダ構成ルール（棚作り）
本フォルダ内は、以下の構造を維持して作業を行うこと。

- `inbox/`    : 未整理の素材、一時的なインプット
- `reference/`: 参考資料、仕様書、過去データ、方針（最優先で読み込む資料）
- `draft/`    : 下書き、設計メモ、中間生成物
- `output/`   : 完成物の出力先
- `archive/`  : 使い終わった古い素材、過去バージョン（※削除禁止。ここへ退避する）

※コード開発系プロジェクトの場合：`src/` や `package.json` 等の「今動いているコード」はルート直下に置いてよい。`draft/` は設計ドキュメントや実装計画メモの置き場、`output/` はビルド成果物の説明やデプロイ情報の置き場として使う（実際のビルド出力は `dist/` や `.next/` などフレームワーク標準の場所のままでよい）。

## 3. 安全ルールと行動規範（事故防止）
1. **ファイルの勝手な削除は禁止**：不要になったファイルは削除せず、必ず `archive/` へ移動する。
2. **変更前の生存確認**：ファイルを変更・上書きする前には、必ず対象ファイルを読み込み、現在の内容を把握する。
3. **事前説明**：ファイルを実際に変更・生成する前に、何を行うかを短く説明する。
4. **事実に基づく生成**：根拠のない数字を捏造しない。不確かな情報は断定せず、未確認である旨を明記する。

## 4. Git運用ルール
1. 意味のある変更のまとまりごとに `git add` → `git commit` を行い、作業の区切りを作ること。
2. `push` ／ `reset --hard` ／force操作など、後戻りしづらい操作はユーザーの明示的な許可なく行わないこと。
3. `.env` 等の秘密情報ファイルは `git add` しないこと（`.gitignore` で既定済み。新しい秘密情報ファイルを作る際も命名パターンに気をつける）。

## 5. 出力スタイルと報告形式
- 回答はすべて日本語で行う。結論から簡潔に述べ、箇条書きを優先し、不要な長文は避ける。
- 不明な点や要件が不足していると感じる場合は、作業前に必ず質問する。
- 依頼された内容以外の追加提案がある場合は、本文に混ぜず末尾に「【追加提案】」として分けて記述する。

作業完了後は、以下の形式で報告する。
```text
### 1. 実施内容
- 変更・作成したファイル（保存パス含む）
- 変更の具体的な中身

### 2. 確認したこと
- 動作確認や品質チェックの結果

### 3. 次のアクション
- 次にやるべきことや推奨される手順
```
